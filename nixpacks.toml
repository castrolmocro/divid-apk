[phases.setup]
nixPkgs = ["nodejs_18", "python3", "ffmpeg"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["node scripts/fix-esm.js"]

[start]
cmd = "node index.js"
